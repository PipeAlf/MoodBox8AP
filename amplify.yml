version: 1
frontend:
  phases:
    preBuild:
      commands: []
    build:
      commands: []
  artifacts:
    baseDirectory: .
    files:
      - '**/*'
      - '!node_modules/**'
      - '!.git/**'
  cache:
    paths: []
